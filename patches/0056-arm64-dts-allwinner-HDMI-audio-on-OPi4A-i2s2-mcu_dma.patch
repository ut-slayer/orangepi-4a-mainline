From 4f5c49460775fe286a2f1734aa86d858de95551b Mon Sep 17 00:00:00 2001
From: Juan Manuel Lopez Carrillo <juanmanuellopezcarrillo@gmail.com>
Date: Fri, 10 Jul 2026 02:16:35 +0200
Subject: [PATCH 056/130] arm64: dts: allwinner: HDMI audio on OPi4A (i2s2 +
 mcu_dma + simple-card)

BSP 5.15 recipe: I2S2@0x7114000 -> DW-HDMI I2S input (internal wiring, dma1
req 5, card sndhdmi, 2x32bit CPU master). i2s2 and mcu_dma nodes taken from
upstream master (A523 I2S series by Chen-Yu Tsai); dma-channels=8 per the BSP
dts (upstream says 16). #sound-dai-cells on the hdmi node + simple-audio-card
rk3328 pattern (mclk-fs 128). Zero code changes: sun4i-i2s (r329 fallback),
sun6i-dma (a100 fallback), dw-hdmi-i2s-audio and hdmi-codec were already
built as modules.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
---
 .../arm64/boot/dts/allwinner/sun55i-a523.dtsi | 28 +++++++++++++++
 .../dts/allwinner/sun55i-t527-orangepi-4a.dts | 34 +++++++++++++++++++
 2 files changed, 62 insertions(+)

diff --git a/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi b/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi
index ede603c8e..25cddf094 100644
--- a/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi
+++ b/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi
@@ -299,6 +299,7 @@ hdmi: hdmi@5520000 {
 			power-domains = <&pck600 PD_VO0>;
 			phys = <&hdmi_phy>;
 			phy-names = "phy";
+			#sound-dai-cells = <0>;
 			status = "disabled";
 
 			ports {
@@ -1000,6 +1001,33 @@ mcu_ccu: clock-controller@7102000 {
 			#reset-cells = <1>;
 		};
 
+		i2s2: i2s@7114000 {
+			compatible = "allwinner,sun55i-a523-i2s",
+				     "allwinner,sun50i-r329-i2s";
+			reg = <0x07114000 0x1000>;
+			interrupts = <GIC_SPI 194 IRQ_TYPE_LEVEL_HIGH>;
+			clocks = <&mcu_ccu CLK_BUS_MCU_I2S2>, <&mcu_ccu CLK_MCU_I2S2>;
+			clock-names = "apb", "mod";
+			resets = <&mcu_ccu RST_BUS_MCU_I2S2>;
+			dmas = <&mcu_dma 5>, <&mcu_dma 5>;
+			dma-names = "rx", "tx";
+			#sound-dai-cells = <0>;
+			status = "disabled";
+		};
+
+		mcu_dma: dma-controller@7121000 {
+			compatible = "allwinner,sun55i-a523-mcu-dma",
+				     "allwinner,sun50i-a100-dma";
+			reg = <0x07121000 0x1000>;
+			interrupts = <GIC_SPI 197 IRQ_TYPE_LEVEL_HIGH>;
+			clocks = <&mcu_ccu CLK_BUS_MCU_DMA>, <&mcu_ccu CLK_MCU_MBUS_DMA>;
+			clock-names = "bus", "mbus";
+			dma-channels = <8>;
+			dma-requests = <15>;
+			resets = <&mcu_ccu RST_BUS_MCU_DMA>;
+			#dma-cells = <1>;
+		};
+
 		npu: npu@7122000 {
 			compatible = "vivante,gc";
 			reg = <0x07122000 0x1000>;
diff --git a/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts b/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts
index 05e49c4c0..dc70a09bd 100644
--- a/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts
+++ b/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts
@@ -675,3 +675,37 @@ &display_clocks {
  * (range invalidation + vendor prefetch) and diagnostic guards
  * against the corrupt DTE from the boot-1 Oops.
  */
+
+/*
+ * HDMI AUDIO (BSP 5.15 recipe, source of truth): the BSP outputs HDMI
+ * audio through I2S2 (i2s2_plat@7114000, dma1 req 5, card "sndhdmi",
+ * i2s format, 2 slots of 32 bits, CPU master, no MCLK) into the DW-HDMI
+ * I2S input (internal SoC wiring, no pinctrl). In mainline the
+ * equivalent is sun4i-i2s (r329 fallback, same register layout) +
+ * dw-hdmi-i2s-audio (the core already registers the codec: CONFIG3 has
+ * GPAUD, verified live) + simple-audio-card, rk3328 pattern. The i2s2
+ * and mcu_dma nodes come from upstream master (A523 I2S series by
+ * Chen-Yu Tsai), except dma-channels=8 which is what the BSP dts says
+ * (upstream puts 16). mclk-fs=128: the DW-HDMI does not consume MCLK
+ * but sun4i-i2s derives the BCLK dividers from it.
+ */
+&i2s2 {
+	status = "okay";
+};
+
+/ {
+	hdmi_sound: hdmi-sound {
+		compatible = "simple-audio-card";
+		simple-audio-card,name = "sndhdmi";
+		simple-audio-card,format = "i2s";
+		simple-audio-card,mclk-fs = <128>;
+
+		simple-audio-card,cpu {
+			sound-dai = <&i2s2>;
+		};
+
+		simple-audio-card,codec {
+			sound-dai = <&hdmi>;
+		};
+	};
+};
